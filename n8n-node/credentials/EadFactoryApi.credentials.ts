// Auto-generated by the g-digital MCP distribution pipeline.
// Source MCP: @g-digital/mcp-ead-factory (1.0.16)
// Architecture: REST-direct (Epic 12 ADR 0008) — Bearer token fetched by the node.

import {
  ICredentialTestRequest,
  ICredentialType,
  INodeProperties,
} from 'n8n-workflow';

export class EadFactoryApi implements ICredentialType {
  name = 'eadFactoryApi';
  displayName = 'EAD Factory API';
  documentationUrl = 'https://api.gcloudfactory.com/chat-bot-manager';

  properties: INodeProperties[] = [
    {
      displayName: 'API Base URL',
      name: 'baseUrl',
      type: 'string',
      default: '',
      description: 'Base URL of the EAD Factory REST API. Leave as-is for the production environment.',
    },
    {
      displayName: 'Auth Email',
      name: 'email',
      type: 'string',
      default: '',
      description: "Your EAD Factory account email (Flow 1). Configure exactly one of Flow 1, 2, or 3.",
    },
    {
      displayName: 'Auth Password',
      name: 'password',
      type: 'string',
      typeOptions: { password: true },
      default: '',
      description: "Your EAD Factory account password (Flow 1, email/password accounts) (See https://digitaltrust.gcloudfactory.com for credential acquisition.)",
    },
  ];

  test: ICredentialTestRequest = {
    request: {
      baseURL: '={{$credentials.baseUrl}}',
      url: '/session',
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: {
        email: '={{$credentials.email}}',
        password: '={{$credentials.password}}',
      },
    },
  };
}
